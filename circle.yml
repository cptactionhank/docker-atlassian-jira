general:
  branches:
    ignore:
      - gh-pages

machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get update && sudo apt-get install jq

test:
  pre:
    - bin/prepare_test

deployment:
  newest-version:
    branch: builder
    commands:
      - bin/deploy
